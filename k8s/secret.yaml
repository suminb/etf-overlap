apiVersion: v1
kind: Secret
metadata:
  name: etf-overlap-secret
  namespace: etf-overlap
type: Opaque
stringData:
  # IMPORTANT: Change these values in production!
  # For production, use: kubectl create secret generic etf-overlap-secret --from-literal=POSTGRES_PASSWORD=your-secure-password
  POSTGRES_PASSWORD: "postgres"
