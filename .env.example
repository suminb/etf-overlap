# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=etf_overlap
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=3000

# Database URL for the app (automatically constructed in docker-compose)
# For local development outside Docker, set this manually:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/etf_overlap
